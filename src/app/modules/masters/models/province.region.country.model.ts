export interface ProvinceOrStateModel {
    id: number;
    description: string;
}

export interface RegionOrCityModel {
    id: number;
    description: string;
}

export interface CountryModel {
    id: number;
    description: string;
}